# 1. Redirection du port 80 vers HTTPS + Challenge Let's Encrypt
server {
    listen 80;
    server_name rva.smce.ovh www.rva.smce.ovh;

    # Dossier où Certbot dépose les fichiers de vérification
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirection automatique vers HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# 2. Votre serveur principal en HTTPS (Port 443)
server {
    listen 443 ssl;
    server_name rva.smce.ovh www.rva.smce.ovh;

    # Certificats SSL générés par Certbot
    ssl_certificate /etc/letsencrypt/live/rva.smce.ovh/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/rva.smce.ovh/privkey.pem;

    # Paramètres SSL recommandés
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    access_log /dev/stdout json_analytics;
    error_log /dev/stderr warn;

    client_max_body_size 20M;
    root /usr/share/nginx/html;
    index index.html;

    proxy_set_header X-Request-ID $http_x_request_id;

    # --- GARDER VOS LOCATIONS IDENTIQUES À AUPARAVANT ---
    location ^~ /img/favicon/ {
        add_header Cache-Control "public, max-age=604800, must-revalidate";
        try_files $uri =404;
    }

    location ^~ /img/banniere/ {
        etag on;
        add_header Cache-Control "no-cache";
        try_files $uri =404;
    }

    location ^~ /img/thomas/ {
        etag on;
        add_header Cache-Control "no-cache";
        try_files $uri =404;
    }

    location /img/ {
        alias /usr/share/nginx/html/img/;
        expires 6m;
        add_header Cache-Control "public, no-transform";
        try_files $uri $uri/ =404;
    }

    location /textes/ {
        alias /usr/share/nginx/html/textes/;
        expires 1d;
        add_header Cache-Control "public, no-transform";
        try_files $uri $uri/ =404;
    }

    # Proxy Express
    location ~* ^/(utilisateurs|autres|articles|courses|statistiques|specialistes|fichiers|images|pages) {
        proxy_pass http://backend:8100;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https; # 👈 Indiquer au backend qu'on est en HTTPS
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Assets
    location ~* \.(?:css|js|gif|jpe?g|png|webp|woff2?|eot|ttf|svg)$ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        access_log off;
    }

    location = /favicon.ico {
        add_header Cache-Control "public, max-age=604800, must-revalidate";
    }

    # SPA React
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}